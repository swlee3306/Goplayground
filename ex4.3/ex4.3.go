package main

import "fmt"

func main() {
	var a int = 3           //3으로 초기화
	var b int               //기본 정수형 값으로 초기화
	var c = 4               //값을 넣어준 값 타입으로 초기화
	d := 5                  //값을 넣어준 타입으로 변수를 만들고 5로 저장
	fmt.Println(a, b, c, d) //출력
}
